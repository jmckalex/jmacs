{
  "name": "@editor/desktop",
  "version": "0.1.0",
  "description": "L0 — the Electron host application that presents the editor.",
  "type": "module",
  "main": "src/main.js",
  "scripts": {
    "dev": "electron .",
    "test": "node --test",
    "smoke": "electron scripts/smoke.js",
    "screenshot": "electron scripts/screenshot.js",
    "screenshot-doc": "electron scripts/screenshot-doc.js"
  },
  "license": "GPL-3.0-or-later",
  "private": true,
  "dependencies": {
    "@editor/buffer": "workspace:*",
    "@editor/lisp": "workspace:*",
    "@editor/pane": "workspace:*",
    "@editor/renderer": "workspace:*",
    "@editor/storage": "workspace:*",
    "@editor/stdlib": "workspace:*",
    "@editor/view": "workspace:*"
  },
  "devDependencies": {
    "electron": "42.2.0"
  }
}
